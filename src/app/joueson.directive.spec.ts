import { JouesonDirective } from './joueson.directive';

describe('JouesonDirective', () => {
  it('should create an instance', () => {
    const directive = new JouesonDirective();
    expect(directive).toBeTruthy();
  });
});
